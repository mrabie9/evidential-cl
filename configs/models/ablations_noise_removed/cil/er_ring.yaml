# Snapshot for the post-noise-removal ablation grid (scripts/run_ablations_noise_removed.sh).
# Baseline: verbatim copy of configs/models/cil/er_ring.yaml as of 2026-09-17 (amp key dropped; the driver forces --amp).
model: er_ring
loader: class_incremental_loader
memory_loss_lambda: 1.0
