# Snapshot for the post-noise-removal ablation grid (scripts/run_ablations_noise_removed.sh).
# Baseline: verbatim copy of configs/models/til/cmaml.yaml as of 2026-09-17 (amp key dropped; the driver forces --amp).
model: lamaml_cifar
loader: task_incremental_loader
learn_lr: false
sync_update: true
use_old_task_memory: true
second_order: false
opt_wt: 0.01
alpha_init: 0.003
